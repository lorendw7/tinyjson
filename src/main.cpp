// tinyjson — Lesson 01: Hello, compiler
//
// This is the entry point of every C++ program. The compiler turns this
// text into a runnable executable. We start here so you can prove your
// toolchain works before we write any JSON code.

#include <iostream> // brings std::cout and std::endl into scope

// main() is where execution begins. It returns an int to the operating
// system: 0 means "success".
int main() {
    std::cout << "tinyjson: toolchain OK" << std::endl;
    return 0;
}
